管理 BiliNote 的后台服务非常简单，主要通过终端命令来完成。由于我们在 WSL2
  中使用了脚本让服务在后台运行，您可以通过以下方式关闭或重启它们：

  🛑 如何关闭服务（停止运行）

  在您的 WSL2 终端中，直接运行以下命令即可强制关闭占用端口的前后端服务：

   1 sudo fuser -k 8483/tcp 3015/tcp
  (系统可能会提示您输入 WSL 用户的密码)

  执行后，FastAPI 后端（8483端口）和 Vite 前端（3015端口）将被彻底关闭。此时您在浏览器中刷新页面会显示无法连接。

  注意： 上述命令不会关闭本地的 Ollama AI 引擎（因为它通常是作为系统级服务常驻的）。如果您想连 Ollama
  一起强制关闭以彻底释放内存，可以额外运行：
   1 pkill ollama

---

  🔄 如何重启服务

  重启最标准的流程就是：先关闭，再启动。

  第 1 步：关闭现有服务

   1 sudo fuser -k 8483/tcp 3015/tcp

  第 2 步：运行一键启动脚本
  确保您在 BiliNote 的项目根目录下，然后执行：
   1 ./start_bilinote.sh

  (由于这个脚本内部已经包含了自动检测并清理 8483 和 3015 端口的逻辑，其实您平时如果只是想重启服务，直接运行
  ./start_bilinote.sh 就可以实现“一键重启”的效果。)